Backend
    Add dowload file
    Add location to file backend
    Add entire Dashboard, save request of user and Location
    Make dashboard - See how many of one request, use OpenAI to accumulate request that sound the same or have the same content
    Improve History of the AI

Frontend
    Adjust cancel so it "seems like" cancellling
        turn request red
    Add evaluation for the individual request
    Clean up code

. I want to introduce a new tab into the section that lewts the user control the locations. So it will be able to create a new location, see all the locations. For that  want to incoperate the map i used in the frontend as information on what are available. Foor that I would like to use google maps completion api which offers me to



   