import { CloseVectorHNSWNode as HNSWLib } from './hnswlib';
import { AxiosProgressEvent } from 'axios';
import { CloseVectorEmbeddings } from 'closevector-common';
export declare function uploadOrUpdateIndexFile(fileUrl: string, options: {
    uuid?: string;
    description: string;
    accessKey: string;
    secret: string;
    uploadUrl: string;
    onProgress?: (progress: AxiosProgressEvent) => void;
}): Promise<void>;
export declare const upsertIndex: (lib: HNSWLib, options: {
    description: string;
    public?: boolean;
    uuid?: string;
    accessKey: string;
    secret: string;
    onProgress?: (progress: AxiosProgressEvent) => void;
}) => Promise<string>;
export declare const getIndex: (options: {
    uuid: string;
    accessKey: string;
    secret: string;
    embeddings: CloseVectorEmbeddings;
    onProgress?: (progress: AxiosProgressEvent) => void;
}) => Promise<HNSWLib>;
