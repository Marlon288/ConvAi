export declare const END_POINT = "https://vector-kv.mega-ug.uk";
export declare enum FileVisibility {
    Public = 0,
    Private = 1
}
export declare const chunkArray: <T>(arr: T[], chunkSize: number) => T[][];
export declare function encryptToken(object: Record<string, any>, secret: string): Promise<string>;
export declare function createUploadFileOperationUrl(options: {
    uuid?: string;
    public?: boolean;
    description: string;
    accessKey: string;
    secret: string;
}): Promise<{
    url: string;
    uuid: string;
}>;
export declare function createGetFileOperationUrl(options: {
    uuid: string;
    accessKey: string;
    secret: string;
}): Promise<{
    url: string;
}>;
